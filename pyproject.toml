[project]
name = "training-pipeline"
version = "0.1.0"
description = "Learning LLM training, optimization and other related stuff"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
quote-style = "double"

[dependency-groups]
dev = [
    "ruff==0.15.22",
]
